project_id = "sweetlight-selfie"

[functions.share-api]
# Public token resolution cannot carry a user JWT. The function authenticates every
# management route itself and uses the service role only for the narrow public read.
verify_jwt = false
